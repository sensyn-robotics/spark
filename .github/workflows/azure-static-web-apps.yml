# Builds and deploys Spark examples site to Azure Static Web Apps
name: Deploy to Azure Static Web Apps

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'examples/**'
      - 'docs/**'
      - 'scripts/**'
      - 'index.html'
      - 'examples.html'
      - 'package.json'
      - '.github/workflows/azure-static-web-apps.yml'
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'src/**'
      - 'examples/**'
      - 'docs/**'
      - 'scripts/**'
      - 'index.html'
      - 'examples.html'
      - 'package.json'
      - '.github/workflows/azure-static-web-apps.yml'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Install MkDocs
        run: pip install mkdocs-material

      - name: Build site
        run: npm run site:build

      - name: Deploy to Azure Static Web Apps
        id: deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_NICE_MEADOW_018297C00 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          skip_app_build: true
          skip_api_build: true
          app_location: "site"
          output_location: ""

      - name: Comment PR with staging URL
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.issue.number;
            const stagingUrl = `https://nice-meadow-018297c00-${prNumber}.6.azurestaticapps.net/`;

            github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🚀 Spark Preview Deployed!\n\n✅ Your changes are live on Azure staging environment.\n\n**🔗 Preview URL:** ${stagingUrl}\n\n**📐 Test Distance Measurement example:**\n${stagingUrl}examples/distance-rescale/\n\n**✨ New Features in This PR:**\n- Infinite rotation (no angle limits)\n- Load PLY button moved to lil-gui\n- Drag & drop file loading\n- Toggle coordinate axes\n- Double-click to set origin\n\n_💡 Tip: Clear your browser cache if you see old content._\n\n_This staging environment will be automatically deleted when the PR is closed._`
            });

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request
    steps:
      - name: Close Azure Static Web Apps staging environment
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_NICE_MEADOW_018297C00 }}
          action: "close"
